language: rust
cache: cargo
dist: bionic
sudo: false
rust:
  - 1.34.0
  - stable
  - beta
install:
  - cargo fetch --verbose
script:
  - cargo check --verbose
  - cargo test --verbose
branches:
  except:
   - /dependabot\/cargo\/.+/
git:
  depth: 5
